export enum goalType {
  NUMBER = 1,
  CHECKLIST = 2,
}

export enum goalCategory {
  UPCOMING = 'Upcoming',
  IN_PROGRESS = 'In Progress',
  DONE = 'Done',
}

export enum color {
  SUCCESS = '#87d068',
  WARNING = '#ffcc00',
  ERROR = '#f50',
}
